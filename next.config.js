/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "www.vectorkhazana.com",
    ],
  },
};

module.exports = nextConfig;
